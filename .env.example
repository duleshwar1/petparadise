# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=1234
DB_NAME=pet_store

# Application Security
# Generate a strong key using: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=super_secret_development_key

# Flask Environment (Set to 'production' for live servers)
FLASK_ENV=development

# Optional: Upload path
UPLOAD_FOLDER=static/images
